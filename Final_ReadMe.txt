For the second task, I added a chromatic aberration post-process effect (which gets applied after the blur). You can control the color channel offsets, as well as a focal point on the screen.

Also note that the blur effect starts at zero. To increase it, use the slider in the IMGUI panel.